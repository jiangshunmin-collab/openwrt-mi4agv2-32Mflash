name: Build Mi 4A Gigabit V2 32M L2TP Kmods

on:
  workflow_dispatch:

env:
  TZ: Asia/Shanghai
  FIRMWARE_VERSION: v24.10.2

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Switch to Official Tag
        run: |
          git checkout tags/${{ env.FIRMWARE_VERSION }}
          echo "Checked out: $(git describe --tags)"

      - name: Initialize Environment
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install build-essential clang flex bison g++ gawk gcc-multilib gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget python3
          sudo timedatectl set-timezone "$TZ"

      - name: Update & Install Feeds
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Patch DTS for 32M Flash (V2 Model)
        shell: python3 {0}
        run: |
          import os
          # 明确指定 V2 的 DTS 路径
          dts_path = "target/linux/ramips/dts/mt7621_xiaomi_mi-router-4a-gigabit-v2.dts"
          if not os.path.exists(dts_path):
              print(f"Error: {dts_path} not found!")
              exit(1)
          with open(dts_path, 'r') as f:
              content = f.read()
          
          # 扩容 Flash 定义
          content = content.replace("<0x0 0x1000000>", "<0x0 0x2000000>")
          
          # 重新定义分区表
          override_node = """
          &partitions {
              partition@180000 {
                  label = "firmware";
                  reg = <0x180000 0x1e70000>;
              };
              partition@1ff0000 {
                  label = "Config";
                  reg = <0x1ff0000 0x10000>;
                  read-only;
              };
          };
          """
          with open(dts_path, 'w') as f:
              f.write(content + "\n" + override_node)
          print("DTS for Mi 4A Gigabit V2 Patched successfully.")

      - name: Configure Firmware
        run: |
          # 1. 硬件平台定义 (V2)
          echo "CONFIG_TARGET_ramips=y" > .config
          echo "CONFIG_TARGET_ramips_mt7621=y" >> .config
          echo "CONFIG_TARGET_ramips_mt7621_DEVICE_xiaomi_mi-router-4a-gigabit-v2=y" >> .config

          # 2. 启用所有内核模块编译
          echo "CONFIG_ALL_KMODS=y" >> .config
          
          # 3. 集成 LuCI 中文界面与 L2TP 协议包
          echo "CONFIG_PACKAGE_luci=y" >> .config
          echo "CONFIG_PACKAGE_luci-i18n-base-zh-hans=y" >> .config
          echo "CONFIG_PACKAGE_luci-proto-ppp=y" >> .config
          echo "CONFIG_PACKAGE_xl2tpd=y" >> .config
          echo "CONFIG_PACKAGE_ppp-mod-pppol2tp=y" >> .config
          echo "CONFIG_PACKAGE_kmod-pppol2tp=y" >> .config

          # 4. 扩容 Rootfs 空间
          echo "CONFIG_TARGET_ROOTFS_PARTSIZE=24" >> .config

          make defconfig

      - name: Download Packages
        run: make download -j8

      - name: Compile Firmware
        run: |
          make -j$(nproc) || make -j1 V=s

      - name: Prepare Assets
        run: |
          mkdir -p ./release_files/
          
          # 1. 提取固件 (递归查找所有 sysupgrade.bin)
          find bin/targets/ -name "*sysupgrade.bin" -exec cp {} ./release_files/ \;
          
          # 2. 暴力提取所有编译出的 .ipk 包 (包括内核包和插件)
          # 使用递归 find 避开具体的路径名称，解决文件夹不存在的问题
          find bin/ -name "*.ipk" -exec cp {} ./release_files/ \;
          
          echo "Total files prepared: $(ls ./release_files/ | wc -l)"
          echo "RELEASE_TAG=Mi4A-V2-32M-${{ github.run_id }}" >> $GITHUB_ENV

      - name: Upload to Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ env.RELEASE_TAG }} \
            --title "Mi 4A Gigabit V2 (32M) - Full Assets" \
            --notes "此 Release 包含了 V2 机型的固件及所有配套驱动包。如果列表中找不到某个包，请点击 Assets 展开。"
          
          # 批量上传所有提取的文件
          gh release upload ${{ env.RELEASE_TAG }} ./release_files/* --clobber
